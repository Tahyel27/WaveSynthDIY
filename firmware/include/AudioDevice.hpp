#pragma once

#include "audio_device.pio.h"

class AudioDevice
{
private:
    /* data */
public:
    AudioDevice(/* args */);
    ~AudioDevice();
};

AudioDevice::AudioDevice(/* args */)
{
}

AudioDevice::~AudioDevice()
{
}
